import 'package:flutter/material.dart';

class RecipeInitialView extends StatelessWidget {
  const RecipeInitialView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Entre les ingredients et trouve ta recete.");
  }
}
